# UI Module Instructions

Read `../../AGENTS.md` first.

This Razor Class Library is intentionally empty in the first vertical. Future
forms and account pages must call the same application services as the HTTP API,
contain no identity business logic, use CSS custom properties as the theming
contract and allow built-in styles to be disabled.
